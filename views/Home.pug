extends layouts/Main
include mixins/videoBlock

block content
    .home-videos
        each item in videos
            +videoBlock({
                title:item.title,
                views:item.views,
                videoFile:item.fileUrl
            })
            